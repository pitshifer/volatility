version: v2

# Единственный модуль — все .proto лежат под proto/.
modules:
  - path: proto

lint:
  use:
    - STANDARD

breaking:
  # Ловит несовместимые изменения контракта (удаление полей/методов,
  # смену типов) — полезно, даже когда клиент один и тот же репозиторий.
  use:
    - FILE
